const mineflayer = require('mineflayer');

function createBot(username) {
  const bot = mineflayer.createBot({
    host: 'rocket33smp.aternos.me', // replace with your IP
    port: 60999,
    username: Yussuf_ABDUL,
    version: false // auto-detect version
